services:

  chat-chronicle:
    container_name: chat-chronicle
    command: python main.py api # or python main.py batch
    build:
      dockerfile: Dockerfile
      context: '.'
    read_only: true
    security_opt:
      - no-new-privileges
    cap_drop:
      - ALL
    ports:
      - '8000:8000'
    volumes:
      - ./input:/app/input:ro
      - ./output:/app/output:rw
      - ./config.yml:/app/config.yml:rw